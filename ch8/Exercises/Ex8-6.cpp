#include<iostream>
#include<print>
#include<cmath>
#include<vector>

void sort(std::vector<unsigned>& numbers);

void getHighest(std::vector<unsigned>& sortedNumbers, unsigned(&highest)[5]);
void getLowest(std::vector<unsigned>& sortedNumbers, unsigned(&lowest)[5]);

doble computeAverage(const std::vector<unsigned>)

int main()
{
  

}
